#include <bits/stdc++.h>
using namespace std;

int N, L, a, b, temp;
set<int> arr;

int main(void) {

    cin >> N >> L >> a >> b;
    for (int i = 0; i <N; i++) {
        cin >> temp;
        arr.emplace(temp);
    }

    for (int i = 0; i < L; i++) {

    }
    
    return 0;
}
